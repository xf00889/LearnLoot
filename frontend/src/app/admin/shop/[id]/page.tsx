"use client";

import { Alert, Box, Button, Chip, Divider, Grid, MenuItem, Paper, Stack, Tab, Tabs, TextField, Typography } from "@mui/material";
import { useParams } from "next/navigation";
import { useEffect, useState } from "react";
import ClientRichTextEditor from "@/components/admin/client-rich-text-editor";
import { createAdminShoppingProduct, deleteAdminShoppingProduct, getAdminShoppingPost, updateAdminShoppingPost, updateAdminShoppingProduct, uploadShoppingPostCover, uploadShoppingProductImage, type AdminShoppingPostDetail, type AdminShoppingProduct } from "@/lib/admin-api";

const emptyProduct = { position: 1, name: "", slug: "", affiliate_url: "", short_description: "", displayed_price: "", original_price: "", currency: "PHP", badge: "", pros: "", cons: "", is_active: true };

export default function AdminShopEditPage() {
  const params = useParams<{ id: string }>(); const [post, setPost] = useState<AdminShoppingPostDetail | null>(null); const [tab, setTab] = useState(0); const [error, setError] = useState(""); const [message, setMessage] = useState(""); const [draftProduct, setDraftProduct] = useState({ ...emptyProduct });
  useEffect(() => { getAdminShoppingPost(params.id).then((value) => { setPost(value); setDraftProduct({ ...emptyProduct, position: value.products.length + 1 }); }).catch((e) => setError(e instanceof Error ? e.message : "Unable to load post.")); }, [params.id]);
  if (!post) return <Stack spacing={2}><Typography variant="h4" fontWeight={900}>Shopping editor</Typography>{error ? <Alert severity="error">{error}</Alert> : <Typography>Loading...</Typography>}</Stack>;

  async function save() { const currentPost = post; if (!currentPost) return; setError(""); setMessage(""); try { const updated = await updateAdminShoppingPost(currentPost.id, { title: currentPost.title, slug: currentPost.slug, post_type: currentPost.post_type, status: currentPost.status, excerpt: currentPost.excerpt, body: currentPost.body, seo_title: currentPost.seo_title, meta_description: currentPost.meta_description, meta_keywords: currentPost.meta_keywords, is_featured: currentPost.is_featured }); setPost(updated); setMessage("Shopping post saved."); } catch (e) { setError(e instanceof Error ? e.message : "Save failed."); } }
  async function addProduct() { const currentPost = post; if (!currentPost) return; setError(""); try { await createAdminShoppingProduct(currentPost.id, draftProduct); const updated = await getAdminShoppingPost(currentPost.id); setPost(updated); setDraftProduct({ ...emptyProduct, position: updated.products.length + 1 }); } catch (e) { setError(e instanceof Error ? e.message : "Unable to add product."); } }
  async function saveProduct(product: AdminShoppingProduct) { const currentPost = post; if (!currentPost) return; setError(""); try { await updateAdminShoppingProduct(product.id, product); setPost(await getAdminShoppingPost(currentPost.id)); } catch (e) { setError(e instanceof Error ? e.message : "Unable to save product."); } }
  async function removeProduct(id: number) { const currentPost = post; if (!currentPost) return; await deleteAdminShoppingProduct(id); setPost(await getAdminShoppingPost(currentPost.id)); }

  return <Stack spacing={2}>
    <Stack direction={{ xs: "column", sm: "row" }} justifyContent="space-between" gap={2}><div><Typography variant="h4" fontWeight={900}>{post.title}</Typography><Typography color="text.secondary">Shopee affiliate editorial CMS</Typography></div><Button variant="contained" onClick={save}>Save changes</Button></Stack>{message ? <Alert severity="success">{message}</Alert> : null}{error ? <Alert severity="error">{error}</Alert> : null}
    <Paper variant="outlined"><Tabs value={tab} onChange={(_, v) => setTab(v)} variant="scrollable"><Tab label="Content" /><Tab label="Products" /><Tab label="SEO" /></Tabs><Divider /><Box sx={{ p: 3 }}>
      {tab === 0 ? <Stack spacing={2}><Grid container spacing={2}><Grid size={{ xs: 12, md: 8 }}><TextField fullWidth label="Title" value={post.title} onChange={(e) => setPost({ ...post, title: e.target.value })} /></Grid><Grid size={{ xs: 12, md: 4 }}><TextField select fullWidth label="Status" value={post.status} onChange={(e) => setPost({ ...post, status: e.target.value })}><MenuItem value="draft">Draft</MenuItem><MenuItem value="published">Published</MenuItem><MenuItem value="archived">Archived</MenuItem></TextField></Grid><Grid size={{ xs: 12, md: 6 }}><TextField fullWidth label="Slug" value={post.slug} onChange={(e) => setPost({ ...post, slug: e.target.value })} /></Grid><Grid size={{ xs: 12, md: 6 }}><TextField select fullWidth label="Post type" value={post.post_type} onChange={(e) => setPost({ ...post, post_type: e.target.value })}><MenuItem value="top_10">Top 10</MenuItem><MenuItem value="flash_deals">Flash deals</MenuItem><MenuItem value="buying_guide">Buying guide</MenuItem><MenuItem value="roundup">Roundup</MenuItem><MenuItem value="article">Article</MenuItem></TextField></Grid></Grid><TextField fullWidth multiline minRows={3} label="Excerpt" value={post.excerpt} onChange={(e) => setPost({ ...post, excerpt: e.target.value })} /><div><Typography fontWeight={800} sx={{ mb: 1 }}>Article body</Typography><ClientRichTextEditor value={post.body} onChange={(body) => setPost({ ...post, body })} minHeight={420} /></div><Stack direction="row" gap={1} alignItems="center"><Button component="label" variant="outlined">Upload cover image<input hidden type="file" accept="image/jpeg,image/png,image/webp,image/avif" onChange={async (e) => { const file = e.target.files?.[0]; if (!file) return; const result = await uploadShoppingPostCover(post.id, file); setPost({ ...post, cover_image_url: result.url }); e.target.value = ""; }} /></Button>{post.cover_image_url ? <Typography variant="caption" color="text.secondary">Cover image configured</Typography> : null}</Stack></Stack> : null}
      {tab === 1 ? <Stack spacing={2}>{post.products.map((product, index) => <Paper variant="outlined" sx={{ p: 2 }} key={product.id}><Stack spacing={1.5}><Stack direction="row" justifyContent="space-between"><Typography fontWeight={800}>#{product.position} {product.name || "Product"}</Typography><Chip size="small" label={product.is_active ? "Active" : "Inactive"} /></Stack><Grid container spacing={1.5}><Grid size={{ xs: 12, md: 2 }}><TextField fullWidth type="number" label="Position" value={product.position} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, position: Number(e.target.value) }; setPost({ ...post, products }); }} /></Grid><Grid size={{ xs: 12, md: 5 }}><TextField fullWidth label="Product name" value={product.name} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, name: e.target.value }; setPost({ ...post, products }); }} /></Grid><Grid size={{ xs: 12, md: 5 }}><TextField fullWidth label="Slug" value={product.slug} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, slug: e.target.value }; setPost({ ...post, products }); }} /></Grid></Grid><TextField fullWidth label="Shopee affiliate URL" value={product.affiliate_url} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, affiliate_url: e.target.value }; setPost({ ...post, products }); }} /><TextField fullWidth multiline minRows={2} label="Short description" value={product.short_description} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, short_description: e.target.value }; setPost({ ...post, products }); }} /><Grid container spacing={1.5}><Grid size={{ xs: 12, md: 3 }}><TextField fullWidth label="Current price" value={product.displayed_price} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, displayed_price: e.target.value }; setPost({ ...post, products }); }} /></Grid><Grid size={{ xs: 12, md: 3 }}><TextField fullWidth label="Original price" value={product.original_price} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, original_price: e.target.value }; setPost({ ...post, products }); }} /></Grid><Grid size={{ xs: 12, md: 2 }}><TextField fullWidth label="Currency" value={product.currency} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, currency: e.target.value }; setPost({ ...post, products }); }} /></Grid><Grid size={{ xs: 12, md: 4 }}><TextField fullWidth label="Badge" value={product.badge} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, badge: e.target.value }; setPost({ ...post, products }); }} /></Grid></Grid><Grid container spacing={1.5}><Grid size={{ xs: 12, md: 6 }}><TextField fullWidth multiline minRows={2} label="Pros (one per line)" value={product.pros} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, pros: e.target.value }; setPost({ ...post, products }); }} /></Grid><Grid size={{ xs: 12, md: 6 }}><TextField fullWidth multiline minRows={2} label="Cons (one per line)" value={product.cons} onChange={(e) => { const products = [...post.products]; products[index] = { ...product, cons: e.target.value }; setPost({ ...post, products }); }} /></Grid></Grid><Stack direction="row" gap={1} flexWrap="wrap"><Button size="small" variant="outlined" onClick={() => saveProduct(post.products[index])}>Save product</Button><Button size="small" component="label" variant="outlined">Upload image<input hidden type="file" accept="image/jpeg,image/png,image/webp,image/avif" onChange={async (e) => { const file = e.target.files?.[0]; if (!file) return; await uploadShoppingProductImage(product.id, file); setPost(await getAdminShoppingPost(post.id)); e.target.value = ""; }} /></Button><Button size="small" color="error" onClick={() => removeProduct(product.id)}>Delete</Button></Stack></Stack></Paper>)}<Divider /><Typography variant="h6" fontWeight={900}>Add product</Typography><Grid container spacing={1.5}><Grid size={{ xs: 12, md: 2 }}><TextField fullWidth type="number" label="Position" value={draftProduct.position} onChange={(e) => setDraftProduct({ ...draftProduct, position: Number(e.target.value) })} /></Grid><Grid size={{ xs: 12, md: 5 }}><TextField fullWidth label="Name" value={draftProduct.name} onChange={(e) => setDraftProduct({ ...draftProduct, name: e.target.value })} /></Grid><Grid size={{ xs: 12, md: 5 }}><TextField fullWidth label="Slug" value={draftProduct.slug} onChange={(e) => setDraftProduct({ ...draftProduct, slug: e.target.value })} /></Grid><Grid size={{ xs: 12 }}><TextField fullWidth label="Shopee affiliate URL" value={draftProduct.affiliate_url} onChange={(e) => setDraftProduct({ ...draftProduct, affiliate_url: e.target.value })} /></Grid></Grid><Button variant="contained" onClick={addProduct} sx={{ alignSelf: "flex-start" }}>Add product</Button></Stack> : null}
      {tab === 2 ? <Stack spacing={2}><TextField fullWidth label="SEO title" inputProps={{ maxLength: 70 }} value={post.seo_title} onChange={(e) => setPost({ ...post, seo_title: e.target.value })} helperText={`${post.seo_title.length}/70`} /><TextField fullWidth multiline minRows={3} label="Meta description" inputProps={{ maxLength: 320 }} value={post.meta_description} onChange={(e) => setPost({ ...post, meta_description: e.target.value })} helperText={`${post.meta_description.length}/320`} /><TextField fullWidth label="Meta keywords" value={post.meta_keywords} onChange={(e) => setPost({ ...post, meta_keywords: e.target.value })} /></Stack> : null}
    </Box></Paper>
  </Stack>;
}
